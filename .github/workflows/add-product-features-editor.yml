name: Rollback Product Save Patch

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rollback-bad-save-patch:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled legacy rollback
        run: echo 'Legacy product-save rollback disabled; keep the current product persistence fix.'

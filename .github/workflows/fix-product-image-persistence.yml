name: Fix product image persistence

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Obsolete product image persistence workflow disabled.'
